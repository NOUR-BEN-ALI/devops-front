---
- name: Build Docker image using Ansible
  hosts: 192.168.33.10
  tasks:
    - name: Build Docker image
      docker_image:
        name: "angularapp"  # Specify your desired image name
        path: .  # Path to the directory containing your Dockerfile
